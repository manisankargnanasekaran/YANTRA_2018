<%= form_with(model: alarm, local: true) do |form| %>
  <% if alarm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alarm.errors.count, "error") %> prohibited this alarm from being saved:</h2>

      <ul>
      <% alarm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :alarm_type %>
    <%= form.number_field :alarm_type, id: :alarm_alarm_type %>
  </div>

  <div class="field">
    <%= form.label :alarm_number %>
    <%= form.number_field :alarm_number, id: :alarm_alarm_number %>
  </div>

  <div class="field">
    <%= form.label :alarm_message %>
    <%= form.text_field :alarm_message, id: :alarm_alarm_message %>
  </div>

  <div class="field">
    <%= form.label :emergency %>
    <%= form.number_field :emergency, id: :alarm_emergency %>
  </div>

  <div class="field">
    <%= form.label :deleted_at %>
    <%= form.datetime_select :deleted_at, id: :alarm_deleted_at %>
  </div>

  <div class="field">
    <%= form.label :machine_id %>
    <%= form.text_field :machine_id, id: :alarm_machine_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
